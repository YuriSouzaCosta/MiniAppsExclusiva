<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0" />
  <title>Contagem de Produtos</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous" />
  <style>
    body {
      background-color: #f4f5f7;
      font-family: Arial, sans-serif;
      padding: 20px;
    }
    .card-container {
      max-width: 500px;
      margin: auto;
    }
    .result p {
      margin: 0;
    }
    .table-size {
      width: 100%;
      margin-top: 20px;
      border-collapse: collapse;
    }
    .table-size th, .table-size td {
      padding: 8px 12px;
      border: 1px solid #dee2e6;
    }
    .table-size thead {
      background-color: #343a40;
      color: white;
    }
  </style>
</head>
<body>
  <div class="card card-container shadow-sm">
    <div class="card-body">
      <h2 class="card-title text-center mb-4">Contagem de Produtos</h2>
      <input class="center form-control" type="text" id="usuario" value="<%= user.username %>" readonly>
      
      <div class="mb-3">
        <label for="codigo" class="form-label">Código de Barras:</label>
        <input type="text" id="codigo" class="form-control" placeholder="Digite o código de barras" />
      </div>
      <div class="d-grid mb-4">
        <button class="btn btn-primary" onclick="buscarProduto()">Buscar</button>
      </div>

      <div class="result mb-4" id="resultado">
        <p><strong>Nome:</strong> <span id="nome">-</span></p>
        <p><strong>Preço:</strong> <span id="preco">-</span></p>
        <p><strong>Ref:</strong> <span id="referencia">-</span></p>
      </div>

      <div class="mb-3">
        <label for="quantidade" class="form-label">Quantidade:</label>
        <input type="number" id="quantidade" class="form-control" placeholder="Digite a quantidade" />
      </div>
      <div class="d-grid mb-2">
        <button class="btn btn-success" onclick="salvarContagem()">Salvar</button>
      </div>
      <% if (user.role === 'ADMIN') { %>
      <div class="d-flex justify-content-end">
        <button class="btn btn-outline-danger small" onclick="zerarContagens()">Zerar Contagens</button>
        <button class="btn btn-outline-secondary small ms-2" onclick="exportarContagens()">Exportar</button>
      </div>
      <% } %>
    </div>
  </div>

  <div class="mt-5">
    <h3 class="text-center">Produtos em Contagem</h3>
    <table class="table-size" id="tabela-contagens">
      <thead>
        <tr>
          <th>Código de Barra</th>
          <th>Produto</th>
          <th>Quantidade</th>
        </tr>
      </thead>
      <tbody>
        <!-- Os produtos serão carregados dinamicamente -->
      </tbody>
    </table>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-fbbOQedDUMZZ5KreZpsbe1LCZPVmfTnH7ois6mU1QK+m14rQ1l2bGBq41eYeM/fS" crossorigin="anonymous"></script>
  <script src="/js/script.js"></script>
</body>
</html>
