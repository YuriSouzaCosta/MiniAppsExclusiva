services:
  app:
    build: .
    container_name: projeto-docker
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/usr/src/app/data   # metas.json persiste entre rebuilds
    security_opt:
      - apparmor=unconfined
